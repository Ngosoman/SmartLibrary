# TODO List for Fixing Multiple Tk() Root Windows

- [x] Change `dashboard = tk.Toplevel()` to `dashboard = tk.Tk()` in the `open_dashboard()` function in dashboard.py to make it the new root window after login.
